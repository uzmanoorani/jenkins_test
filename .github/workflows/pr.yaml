name: "Deploy on Comment"


on: 
  pull_request:
    types:
      - opened
      - synchronize
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Deploy
        run: |
          echo "Deploying..."