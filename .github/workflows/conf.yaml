name: Configuration test
on:
  workflow_dispatch:
    inputs:
      ENV:
        description: 'Deployment Environment'
        default: 'dev'
        # type: choice
        # required: true
        # options:
        #   - dev
        #   - stage
        #   - prod
jobs:
  check:
    name: Run check
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo Hello